name: acceptable risk exceeded
type: incident-triggering
purpose: 
prerequisites: 
- indexes: 
          anomaly_collection (explanation: an index where identified anomalies are stored as events)
          incident_collection (explanation: an index where identified incidents are stored as events)
- lookups: 
- macros: 
- fields: 
          title
          description
          staff_ID
          patient_ID
          risk_score
          drilldown_search
- searches: 
          search 1
how_to_implement:
          1. create an index called 'anomaly_collection' and 'incident_collection'. 
          5. create 'search 1' as a report and then schedule it according to the instructions below. 
search 1: 
- description: 
- search: 
          index=anomaly_collection earliest=-24h
          | eventstats sum(risk_score) by staff_ID
          | stats sum(risk_score) values(drilldown_search) by staff_ID
          | collect index=incident_collection 
- time range picker: 
- cron schedule: 
